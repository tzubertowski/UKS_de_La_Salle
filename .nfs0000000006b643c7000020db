\documentclass{article}
\usepackage[utf8]{inputenc}
\usepackage{polski}
\usepackage{amsmath}
\usepackage{graphicx}

\title{UKS de La Salle}
\author{Tomasz Szymanek}
\and
\author{Tomasz Żubertowski}

\date{Październik 2013}

\usepackage{natbib}
\usepackage{graphicx}

\begin{document}

\maketitle

\section{Opis zadania.}
\[\int\limits^a_b \, e^{x^{2}} dx \] 
Zadaniem było zaimplementowanie dwóch programów: sekwencyjnego i równoległego. Oba z tych programów zostały zaprojektowane tak, aby otrzymać przybliżoną wartość całki poprzez zsumowanie policzonych pół trapezów w danym przedziale

Ponadto do zadania zostały podane dane:
\[ a\,\,=,\,\,-2;\]
\[ b\,\,=,\,\,2;\]
\[ N\,\,=,\,\,512;\]
Gdzie N to liczba x w przedziale pomiędzy którymi będziemy liczyć pola trapezów.

\section{Rozwiązanie zadania.}
\subsection{Rozwiązanie sekwencyjne}
Dla standardowych danych podanych w zadaniu (w/w) przyrost czasu jest praktycznie stały. Sytuacja ma się jednak inaczej w przypadku, w którym programowi podamy większe N - widzimy jak niewydajmym rozwiązaniem jest program sekwencyjny, czasy rosną zdecydowanie szybciej aniżeli w przypadku programu równoległego.

\subsection{Rozwiązanie równoległe}
Dla standardowych danych podanych w zadaniu program pisany w technologii MPI, uruchamiany na 4 wątkach okazuje się być, zgodnie z logiką zadania, okazuje się szybszy. Jeżeli rozpatrujemy dużo mniejszą liczbę N sytuacja się odwraca dlatego, że wielowątkowość w krótkim okresie czasu życia programu dorzuca jeszcze swój czas potrzebny na obsługę wątków.

\begin{figure}[h!]
  \caption{Porównanie czasów działania.}
  \centering
    \includegraphics[width=0.7\textwidth]{czas.png}
\end{figure}

\bibliographystyle{plain}
\bibliography{references}
\end{document}
